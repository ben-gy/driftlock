{
  "name": "driftlock",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "You never move a stone — you move the row it stands on, and whatever slides into the Well is yours.",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "trystero": "^0.21.0"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "vite": "^6.0.0",
    "vitest": "^3.2.6",
    "@types/node": "^22.0.0",
    "jsdom": "^25.0.0"
  }
}
